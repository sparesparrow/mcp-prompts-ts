{
  // Docker daemon address pools for custom bridge network management.
  // Adjust these ranges if you have network conflicts or need to isolate containers.
  "default-address-pools": [
    {
      "base": "172.30.0.0/16",
      "size": 24
    },
    {
      "base": "172.31.0.0/16",
      "size": 24
    }
  ]
}
