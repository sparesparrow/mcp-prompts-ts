{
  "name": "mcp-prompts-ts",
  "private": true,
  "workspaces": ["packages/*", "apps/*"],
  "devDependencies": {
    "typescript": "^5.5.0",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0",
    "vitest": "^1.0.0"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  }
}
